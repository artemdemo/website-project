{
  "name": "website-playground",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "yarn clear:build; site-builder build",
    "clear:build": "rm -rf target; rm -rf build; rm -rf dist"
  },
  "devDependencies": {
    "site-builder": "workspace:*"
  }
}
